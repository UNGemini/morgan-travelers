Bus-shape overrides are loaded at runtime from `VITE_OVERRIDES_BUS_SHAPES_URL` (dev), `/api/overrides/bus-shapes.json` (prod Pages Function proxy), or GitHub raw `UNGemini/morgan-travelers-overrides`; call `invalidateBusShapeOverridesCache()` / `reloadBusShapeOverrides()` to refresh during a session. Place search requires a running `/geocode` proxy because Nominatim countrycode filtering is unreliable for HK.